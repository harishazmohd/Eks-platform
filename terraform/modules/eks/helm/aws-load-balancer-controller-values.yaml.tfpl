clusterName: "${cluster_name}"
region: "${region}"
vpcId: "${vpc_id}"
serviceAccount:
  create: true
  name: "${service_account_name}"
  annotations:
    eks.amazonaws.com/role-arn: "${aws_iam_role_arn}"
replicaCount: 2
